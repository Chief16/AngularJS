// import credentials from "../dal/credentials"

function validateLogin(email, password){
    // const user = credentials.find(u => u.name == email);
    // if(!user) return user;
    // return user.password == password;
    return true;
}